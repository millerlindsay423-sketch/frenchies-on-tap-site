# 2026-07-20

## What we covered

- Palmer's Monday Instagram post (`palmer-bar-interrogation.jpg`) had stalled. Confirmed dog identity (pink bandana = Palmer), drafted the caption, and published it — media ID `18106945178094724`.
- Set up this status-log convention in `CLAUDE.md` so a new chat can get caught up from this folder instead of re-explaining everything, and this file itself as the first real example.

## Open website production items (uncommitted/unpushed)

Ready to commit as-is, just need a look/go-ahead:
- **Lazy Guy Distillery page update** (`breweries/lazy-guy-distillery.html`) — fixes the server's name typo ("Tracy" → "Tracey"), adds a "two years later" update paragraph about winning **Palmer's Pick 2025**, and wires in 2 new photos of the award (`images/breweries/lazy-guy-distillery-5.jpg`, `-6.jpg`, currently untracked). This looks like a complete, matched set of changes from a prior session — worth a read-through before it ships since none of this happened in this conversation.
- **`scripts/instagram/post.js` enhancement** — adds carousel support (multiple comma-separated image URLs → swipeable post) and a `--tag` flag for mentioning Instagram usernames on a photo. Also made outside this session; looks complete/functional but hasn't been tested live yet as far as I know.

Needs a decision before it can move:
- **`images/dogs/IMG_4013.jpg` — deleted.** Shows as removed in git (file's gone from disk) but it was previously committed. Unresolved from a prior session. Recoverable via `git checkout` from git history if you want it back; otherwise it can just be committed as a deletion.

Lower priority, no rush:
- A batch of new, untagged photos sitting in `images/dogs/` — not yet tagged (Red/Orange/Yellow) or sorted into the gallery/Instagram pipeline: `1000013953.jpeg`, `609.jpeg`, `IMG_2146.jpg`, `IMG_2388.jpg`, `IMG_2580.jpg`, `IMG_2587.jpg`, `IMG_3722.jpg`, `IMG_4506.jpg`, `IMG_5397.jpg`, `IMG_5551.jpg`, `Lazy guy 2.jpg`, `Rose tongue.jpeg`, `lazy guy 1.jpg`. (`New Website.jpg` is also untracked here, but that's expected — it's already live via `images/gallery/new-website-portrait.jpg`; the original source file just isn't committed itself.)
- Two untracked files in `images/logo/` (`Card QR Cropped.png`, `Frenchies Est 2023 (1).jpg`) — these predate this conversation and no session has touched them; unclear what they're for or whether they're still needed. Flagging since they're the kind of thing that's easy to lose track of.
- This `status/` folder itself isn't committed yet.

## Open questions for Lindsay

- OK to commit the Lazy Guy Distillery update + `post.js` enhancement as-is, or want to review the actual copy/code first?
- Restore `images/dogs/IMG_4013.jpg`, or is its removal intentional?
- Any priority on tagging/sorting the batch of new untagged photos, or fine leaving them for whenever?
- What are `Card QR Cropped.png` and `Frenchies Est 2023 (1).jpg` for — still needed, or safe to ignore/clean up?
